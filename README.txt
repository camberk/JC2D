The backend to this project is using Firebase Realtime Database

In order to run this project locally:

1. npm install firebase
2. npm install -g firebase-tools
3. firebase serve   # Start development server
